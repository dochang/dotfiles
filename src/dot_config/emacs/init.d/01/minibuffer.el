(setup minibuffer

  (:when-loaded

    (setopt read-file-name-completion-ignore-case nil)
    ;; For `read-file-name-completion-ignore-case', do not ignore case.
    ;;
    ;; Try to respect the real names of files.

    )

  )
