---

- name: Install utils
  command: >-
    go get
    {{ (pkg_state == "latest") | ternary("-u", "") }}
    {{ item }}
  environment:
    GOENV_VERSION: system
    CGO_ENABLED: 0
  with_items:
    - '{{ envs.go_get_packages }}'
